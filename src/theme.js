const primaryOrange = '#FF8300';
const primaryBlue = '#1D24AB';
const secondaryOrange = '#FFB466';
const liteOrange = '#FFE6CC';
const white = '#FFFFFF';
const grey = '#666F70';
const lightGrey = 'rgba(255, 255, 255, 0.6)';

const theme = {
  primaryBlue,
  primaryOrange,
  secondaryOrange,
  liteOrange,
  white,
  grey,

  systemColor: primaryOrange,
  navBar: primaryOrange,
  statusBar: primaryOrange,
  tabBar: primaryOrange,
  tabBarBackgroundColor: primaryOrange,
  tabBarButtonColor: lightGrey,
  tabBarSelectedButtonColor: white,
};

export default theme;
